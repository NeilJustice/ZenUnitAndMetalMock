{
   "version": "0.2.0",
   "configurations":
   [
      {
         "name": "Debug ZenUnitPyTests",
         "type": "cppdbg",
         "request": "launch",
         "cwd": "${workspaceRoot}",
         "program": "${currentFile}"
      }
   ]
}